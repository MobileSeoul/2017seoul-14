<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:ignore="missingPrefix">

<include
        layout="@layout/my_menu_toolbar" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="20dp"
        android:paddingRight="19dp"
        android:paddingLeft="19dp"
        >
    <ImageView
        android:layout_width="322dp"
        android:layout_height="290dp"
        android:scaleType="center"
        android:src="@drawable/camera_ic"
        android:id="@+id/regit_image"
        android:background="@drawable/grey_rounded_back"
        android:layout_gravity="center_horizontal"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_marginTop="11dp"
        android:padding="7dp"
        android:paddingStart="10dp"
        android:inputType="text"
        android:textSize="15dp"
        android:id="@+id/regit_text"
        android:gravity="top"
        android:layout_gravity="center_horizontal"
        app:font="Regular"
        android:hint="(20자 이내로 작성해주세요)"
        android:textCursorDrawable="@drawable/pink_cursor"
        android:background="@drawable/rounded_line_edittext"/>
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <Button
                android:id="@+id/regit_btn"
                android:layout_width="match_parent"
                android:layout_height="42dp"
                android:layout_alignParentBottom="true"
                android:layout_marginBottom="40dp"
                android:textColor="#FFFFFF"
                app:font="Regular"
                android:text="등 록 하 기"
                android:textSize="17dp"
                android:background="@drawable/pink_back_btn"
                android:layout_centerInParent="true"/>
        </RelativeLayout>
       </LinearLayout>
</LinearLayout>